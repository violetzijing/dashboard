== Dashboard

Dashboard is made for display test results from QADB. It can analyse results and display them as charts and lines.

Currently it can display test results according to build info and time.

== Installation

`git clone git@github.com:violetzijing/dashboard.git`
`bundle exec rails s`

== Next

* Use MySQL
* Display and analyse regression test results
* Display performance test results
